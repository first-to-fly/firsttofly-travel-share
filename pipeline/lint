#!/usr/bin/env bash

source "./lib/bash/core.sh"

dependency "shellcheck"

function main() {

  echo -e "${OK_COLOR}==> Linting code..."

  (
    set -x
    shellcheck \
      --external-sources \
      "./.bin/"* \
      "./lib/bash/"* \
      "./pipeline/"*
  )

}

main "$@"
